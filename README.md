# 一些用于 AI 的配置手册

本仓库收集用于 AI 的配置手册与可复用技能。

## nat64-relay

这是一个基于 Tailscale 的 split DNS + NAT64 中继配置手册。它用于让普通主机名保持直连，同时让特定后缀（如 `.9929`）的流量强制走中继节点。

示例：
- `ping my-vps`：目标主机在国外，你自己在国内，默认直连路径可用但性能一般。
- `ping my-vps.9929`：通过 9929 中继节点转发后，链路质量更稳定、实际性能更好。
